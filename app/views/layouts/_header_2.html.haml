%nav
  .nav-wrapper
    %a#sidenav.button-collapse{"data-activates" => "slide-out", "data-turbolinks" => "false", :href => "#"}
      %i.material-icons menu
    %a.brand-logo{:href => "/"}
      = image_tag("logo.png", height: '65', style:"margin-top: 0px;")
    %ul.right.hide-on-med-and-down
      = render 'layouts/menu'
      - if user_signed_in?
        %li
          %a.dropdown-button{"data-activates" => "dropdown1", :href => "#!"}
            %span
              = current_user.email
            %i.material-icons.right arrow_drop_down
      - else
        %li
          = link_to ('LOGIN'), new_user_session_url
      - if user_signed_in?
        %ul#dropdown1.dropdown-content
          %li
            = link_to ('HIBAH-Ku'), hibahs_url
          %li.divider
          %li
            = link_to ('PROFILE'), edit_user_registration_url
          %li.divider
          %li
            = link_to ('LOGOUT'), destroy_user_session_url, data: {method: :delete}
